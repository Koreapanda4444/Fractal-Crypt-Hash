#include "fch.h"
#include "fractal.h"
#include "params.h"

void fch_hash_256(
    const uint8_t *input,
    size_t length,
    uint8_t output[32]
) {
    (void)input;
    (void)length;
    (void)output;
}

void fch_hash_512(
    const uint8_t *input,
    size_t length,
    uint8_t output[64]
) {
    (void)input;
    (void)length;
    (void)output;
}
